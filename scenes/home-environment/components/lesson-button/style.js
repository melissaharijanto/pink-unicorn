import { StyleSheet } from "react-360";

export default StyleSheet.create({
  text: {
    backgroundColor: `${colors.three}`,
    textAlign: "center",
    color: `${colors.one}`,
    marginTop: 30,
    borderRadius: 20,
  }
});
